name: Nightly jobs

on:
  workflow_dispatch:

jobs:
  phase-8-placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Nightly backup validation and smoke tests are configured in Phase 8."
