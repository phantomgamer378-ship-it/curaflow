name: Production deploy

on:
  workflow_dispatch:

jobs:
  phase-8-placeholder:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - run: echo "Production deployment is configured in Phase 8."
